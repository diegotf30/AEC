<% if flash.any? %>
  <% flash.each do |type, body| %>
    <% next unless %w(success notice error info).include?(type.to_s) %>
    <% if body.kind_of?(Array) %>
      <% body.each do |message| %>
        <% single_line_flash(type, message) %>
      <% end %>
    <% else %>
        <% single_line_flash(type, body) %>
    <% end %>
  <% end %>
  <% flash.clear %>
<% end %>
